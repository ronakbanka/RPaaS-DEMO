<%= form_for @comment, :url => topic_comments_path, :id => params[:topic_id] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.submit "create" %>
<% end %>
